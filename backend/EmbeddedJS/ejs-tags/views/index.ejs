<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS Tags</title>
</head>
<body>
    <h1><%= title %></h1>
    <p>Current second: <%= seconds %></p>
    <ul>
    <% if (seconds % 2 === 0){ %>
    <% for(let i = 0; i < items.length; i++){ %>
            <li>  <%=items[i]%> </li> 
        <% } %>
    <%} else { %>
         <%- "No items to show" %>
    <% } %>
    </ul> 
    <p><%- htmlContent %></p> <!-- This is the method if you want to have html tags to preview in HTML -->

</body>
<%- include('footer') %>
</html>